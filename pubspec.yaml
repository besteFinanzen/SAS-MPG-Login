name: sas_login_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  confetti: ^0.7.0
  file_selector: ^1.0.3
  intl: ^0.19.0
  path_provider: ^2.1.3
  simple_barcode_scanner:
    git:
      url: https://github.com/besteFinanzen/simple_barcode_scanner
      ref: main
  permission_handler: ^11.3.1
  flutter_file_dialog: ^3.0.2
  file_picker: ^8.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
